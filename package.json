{
  "name": "panorama",
  "private": true,
  "version": "1.0.4",
  "description": "A Windows Explorer-inspired file manager for desktop",
  "scripts": {
    "dev": "cd flutter_app && flutter run -d macos",
    "start": "cd flutter_app && flutter run -d macos",
    "build": "cd flutter_app && flutter build macos",
    "test": "cd flutter_app && flutter test",
    "analyze": "cd flutter_app && flutter analyze",
    "install:mac": "./scripts/install-mac.sh"
  }
}
